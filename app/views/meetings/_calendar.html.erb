<%= month_calendar events: meetings do |dates, meetings| %>
    <%= date %>
    <% meetings do |meeting| %> 
    <div>
        <% link_to meeting.name, meeting, class; "link"%>
    </div>
    <% end %>
<% end %>